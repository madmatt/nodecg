doctype html
html(lang="en")
    head
        meta(http-equiv="X-UA-Compatible", content="IE=edge; charset=UTF-8")
        meta(name="viewport", content="width=device-width", initial-scale=1)

        title NodeCG login

        link(rel='stylesheet', href='/auth/login.css')

    body
        div.container
            form(class='form-signin', role='form')
                h2.form-signin-heading Please sign in
                if user !== undefined && !user.allowed
                    div(class='alert alert-danger')
                        strong Access denied!
                        br
                        | Your account is not in the whitelist
                if config.auth.steam.enabled
                    a(href='/auth/login/steam')
                        img(src='/auth/signin_steam.png')
                if config.auth.twitch.enabled
                    a(href='/auth/login/twitch')
                        img(src='/auth/signin_twitch.png')
